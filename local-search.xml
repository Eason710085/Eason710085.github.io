<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>myfirstblog</title>
    <link href="/2021/03/10/myfirstblog/"/>
    <url>/2021/03/10/myfirstblog/</url>
    
    <content type="html"><![CDATA[<span id="more"></span><p>in this blog, i am gonna show some examples about pointers in c<br>Address in C<br>If you have a variable var in your program, &amp;var will give you its address in the memory.</p><p>We have used address numerous times while using the scanf() function.</p><p>scanf(“%d”, &amp;var);<br>Here, the value entered by the user is stored in the address of var variable. Let’s take a working example.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdio.h&gt;</span></span><br><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>  <span class="hljs-keyword">int</span> var = <span class="hljs-number">5</span>;<br>  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;var: %d\n&quot;</span>, var);<br><br>  <span class="hljs-comment">// Notice the use of &amp; before var</span><br>  <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;address of var: %p&quot;</span>, &amp;var);  <br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>Output</p><p>var: 5<br>address of var: 2686778<br>Note: You will probably get a different address when you run the above code.</p><p>C Pointers<br>Pointers (pointer variables) are special variables that are used to store addresses rather than values.</p><p>Pointer Syntax<br>Here is how we can declare pointers.</p><p>int* p;<br>Here, we have declared a pointer p of int type.</p><p>You can also declare pointers in these ways.</p><p>int *p1;<br>int * p2;<br>Let’s take another example of declaring pointers.</p><p>int* p1, p2;<br>Here, we have declared a pointer p1 and a normal variable p2.</p><p>Assigning addresses to Pointers<br>Let’s take an example.</p><p>int* pc, c;<br>c = 5;<br>pc = &c;<br>Here, 5 is assigned to the c variable. And, the address of c is assigned to the pc pointer.</p><p>Get Value of Thing Pointed by Pointers<br>To get the value of the thing pointed by the pointers, we use the * operator. For example:</p><p>int* pc, c;<br>c = 5;<br>pc = &c;<br>printf(“%d”, *pc);   // Output: 5<br>Here, the address of c is assigned to the pc pointer. To get the value stored in that address, we used *pc.</p><p>Note: In the above example, pc is a pointer, not *pc. You cannot and should not do something like *pc = &c;</p><p>By the way, * is called the dereference operator (when working with pointers). It operates on a pointer and gives the value stored in that pointer.</p><p>Changing Value Pointed by Pointers<br>Let’s take an example.</p><p>int* pc, c;<br>c = 5;<br>pc = &c;<br>c = 1;<br>printf(“%d”, c);    // Output: 1<br>printf(“%d”, *pc);  // Ouptut: 1<br>We have assigned the address of c to the pc pointer.</p><p>Then, we changed the value of c to 1. Since pc and the address of c is the same, *pc gives us 1.</p><p>Let’s take another example.</p><p>int* pc, c;<br>c = 5;<br>pc = &c;<br>*pc = 1;<br>printf(“%d”, *pc);  // Ouptut: 1<br>printf(“%d”, c);    // Output: 1<br>We have assigned the address of c to the pc pointer.</p><p>Then, we changed *pc to 1 using *pc = 1;. Since pc and the address of c is the same, c will be equal to 1.</p><p>Let’s take one more example.</p><p>int* pc, c, d;<br>c = 5;<br>d = -15;</p><p>pc = &c; printf(“%d”, *pc); // Output: 5<br>pc = &d; printf(“%d”, *pc); // Ouptut: -15</p><p>Initially, the address of c is assigned to the pc pointer using pc = &c;. Since c is 5, *pc gives us 5.</p><p>Then, the address of d is assigned to the pc pointer using pc = &d;. Since d is -15, *pc gives us -15.</p><p>Example: Working of Pointers<br>Let’s take a working example.</p><p>#include &lt;stdio.h&gt;<br>int main()<br>{<br>   int* pc, c;</p><p>   c = 22;<br>   printf(“Address of c: %p\n”, &amp;c);<br>   printf(“Value of c: %d\n\n”, c);  // 22</p><p>   pc = &c;<br>   printf(“Address of pointer pc: %p\n”, pc);<br>   printf(“Content of pointer pc: %d\n\n”, *pc); // 22</p><p>   c = 11;<br>   printf(“Address of pointer pc: %p\n”, pc);<br>   printf(“Content of pointer pc: %d\n\n”, *pc); // 11</p><p>   *pc = 2;<br>   printf(“Address of c: %p\n”, &amp;c);<br>   printf(“Value of c: %d\n\n”, c); // 2<br>   return 0;<br>}<br>Output</p><p>Address of c: 2686784<br>Value of c: 22</p><p>Address of pointer pc: 2686784<br>Content of pointer pc: 22</p><p>Address of pointer pc: 2686784<br>Content of pointer pc: 11</p><p>Address of c: 2686784<br>Value of c: 2</p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
